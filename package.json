{
  "name": "url-shortener",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Minimal HTTP URL shortener with a Vercel-hosted React frontend and Express API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.16.3"
  }
}
